/** Shared horizontal page padding — fluid from phone → desktop. */
export const sectionPadX =
  "px-5 sm:px-8 md:px-12 lg:px-16 xl:px-[120px] 2xl:px-[200px]";

/** Shared vertical section padding. */
export const sectionPadY = "py-16 sm:py-20 lg:py-[100px]";

export const sectionPad = `${sectionPadX} ${sectionPadY}`;
